Fabric3 Spring Profile Installation
------------------------------------

1. Copy the contents of /extensions to the Fabric3 /extensions or runtime/<runtime name>/extensions directory.

2. Remove the fabric3-security-impl extension if previously installed in the runtime OR remove the fabric3-spring-security extension if
   Spring Security is not being used.

3. Start the Fabric3 server.



